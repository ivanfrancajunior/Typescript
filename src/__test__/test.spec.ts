describe("this should be work", () => {
  it("expect one to be one", () => {
    expect(1).toBeGreaterThan(0);
  });
  it("expect one to be one", () => {
    const string = "abacate";
    expect(string).toBe("abacate");
  });
});
